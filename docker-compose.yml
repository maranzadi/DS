#version: "3.9"

services:
  devkitarm:
    image: devkitpro/devkitarm:latest
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: sh -c "dkp-pacman -Sy --noconfirm nds-dev && make clean && make"